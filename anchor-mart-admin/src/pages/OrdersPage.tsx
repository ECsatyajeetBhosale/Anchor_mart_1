// Thin re-export wrapper — the implementation lives in the orders feature
// (`features/orders/components/OrdersPage.tsx`); routing imports it from here
// unchanged.
export { OrdersPage, OrdersPage as default } from "@/features/orders";
