// Thin re-export wrapper. The Sailors feature lives in `features/sailors/`
// (component-driven + RTK Query); routing imports it from here unchanged.
export { SailorsPage } from "@/features/sailors";
