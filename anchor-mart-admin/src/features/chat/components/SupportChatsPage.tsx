import { ChatMonitorPage } from "./ChatMonitorPage";

/** Support-desk threads — the "Support" nav entry. */
export function SupportChatsPage() {
  return <ChatMonitorPage source="support" />;
}

export default SupportChatsPage;
